# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets). Every PR that changes package behaviour must include a changeset describing the semver bump.

```bash
pnpm changeset
```
